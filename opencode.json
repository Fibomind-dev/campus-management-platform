{
  "$schema": "https://opencode.ai/config.json",

  "permission": {
    "edit": "ask",
    "bash": "ask",
    "external_directory": "deny"
  },

  "agent": {
    "odoo": {
      "description": "Odoo development agent. Works only on Odoo/custom addon concerns.",
      "mode": "primary",
      "permission": {
        "edit": "ask",
        "bash": "ask"
      }
    },

    "postgresql": {
      "description": "PostgreSQL/database agent. Database work is isolated from Odoo addon work.",
      "mode": "primary",
      "permission": {
        "edit": "ask",
        "bash": "ask"
      }
    },

    "reviewer": {
      "description": "Read-only reviewer. Reviews proposed changes without modifying files.",
      "mode": "subagent",
      "permission": {
        "edit": "deny",
        "bash": "deny"
      }
    }
  }
}
